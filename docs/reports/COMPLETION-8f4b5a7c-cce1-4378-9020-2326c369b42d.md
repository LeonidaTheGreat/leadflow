# Completion Report: TEST FILE PLACEMENT rule — Dev Agent Instructions

**Task ID:** 8f4b5a7c-cce1-4378-9020-2326c369b42d
**Status:** completed
**Date:** 2026-06-11

## Root Cause Analysis

- **Failure point:** Dev agent was placing test files in `product/lead-response/dashboard/tests/` instead of the project-root `tests/` directory.
- **Why:** No explicit rule in agent instructions about test file placement paths. Agents defaulted to placing tests near the code being tested (Next.js dashboard), which has its own subdirectory.
- **Fix:** Added TEST FILE PLACEMENT rule to three canonical locations so all dev spawns receive it.

## Changes Made

### 1. SOUL.md — `/Users/clawdbot/.openclaw/workspace-dev/SOUL.md`
Added at line 65 in the "What You Don't Do" section:
```
- **TEST FILE PLACEMENT (mandatory).** ALL test files MUST be under the project-root `tests/` directory: `tests/e2e/`, `tests/integration/`, or `tests/unit/`. NEVER place tests in `product/lead-response/dashboard/tests/` or any directory outside `tests/`. After writing a test file, verify its path starts with `tests/` before committing. Verification will reject test files outside `tests/` and fail the task.
```

### 2. `role-context.js` — `/Users/clawdbot/projects/genome/core/food/role-context.js`
Rule inserted in `buildRoleContext()` for the `dev` role at lines 376-379 (deliverable steps section) and 395-396 (PROJECT STRUCTURE section):
- Step 2 of deliverables now has mandatory TEST FILE PLACEMENT sub-rules
- PROJECT STRUCTURE section reinforces: "Tests → project-root `tests/e2e/`, `tests/integration/`, or `tests/unit/` ONLY"

### 3. `workflow-engine.js` — `/Users/clawdbot/projects/genome/intelligence/workflow-engine.js`
Added canonical comment at lines 198-202 documenting that `role-context.js` is the authoritative source for the TEST FILE PLACEMENT policy.

## Verification

```
node -e "const { buildRoleContext } = require('/Users/clawdbot/projects/genome/core/food/role-context'); const out = buildRoleContext('dev','Implement: demo','desc'); console.log(/TEST FILE PLACEMENT/.test(out.spawnRole) ? 'PASS' : 'FAIL');"
# Output: PASS

PASS: TEST FILE PLACEMENT found in spawnRole
PASS: tests/e2e found in spawnRole  
PASS: dashboard/tests warning present in spawnRole
```

## Test Results

- Passed: 3 / 3 (100%)
- buildRoleContext() returns TEST FILE PLACEMENT rule: ✅
- Rule covers tests/e2e, tests/integration, tests/unit: ✅
- Rule warns against dashboard/tests: ✅

## Genome Commits

Changes are committed to the genome repo (`~/projects/genome/`):
- `22e9624` — docs: SPEC WCP-8 (Quality Ratchet) — initial TEST FILE PLACEMENT addition to role-context.js and workflow-engine.js
- Subsequent commits maintained and extended the rule without removing it
