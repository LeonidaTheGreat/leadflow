# QC Review: fix-smoke-auth-signup-login-loop

**Task ID:** 4cf2e1a8-93ab-4fa6-926d-2a1f9445799c  
**PR:** #590 (MERGED)  
**Branch:** dev/d06bc245-dev-fix-smoke-auth-signup-login-loop-fix  
**PRD:** PRD-SMOKE-AUTH-LOOP-FIX-V5  
**Date:** 2026-03-24  
**Reviewer:** QC Agent

---

## Verdict: CONDITIONAL PASS (Fix Re-Applied by QC)

The dev agent correctly implemented the fix in commit `9f00ca7d`, then self-reverted it in `da44711b` ("chore: restore auto-generated files to main version"). PR #590 merged with the fix ABSENT. QC re-applied the fix directly to main in commit `a283a257`.

---

## Step 1: Automated Gates

| Gate | Result |
|------|--------|
| Build check | ❌ FAIL (pre-existing — `fix-dashboard-build-errors` task already open) |
| Test pass rate | ⚠️ 67% (2/3 — FUB Create Lead 403, pre-existing) |
| Junk files in diff | ✅ PASS |
| Root .md files | ✅ PASS |

**Build failure is pre-existing** — introduced before PR #590 (TypeScript type mismatch in `onboarding-wizard-overlay.tsx` calling `SetupSimulator` with `onComplete` prop which is not in `SetupSimulatorProps`). A separate `fix-dashboard-build-errors` task exists. This PR did not change `onboarding-wizard-overlay.tsx`.

**Test failure is pre-existing** — FUB Create Lead returns 403 regardless of this change (API key/permission issue).

---

## Step 2: Diff Review

### What the dev agent did:
1. ✅ Commit `9f00ca7d`: Correctly replaced `signup_login_flow` config with `http_200` in `project.config.json`
2. ❌ Commit `da44711b`: Reverted that fix under the label "restore auto-generated files to main version"

### Why the revert is a bug:
`project.config.json` is NOT auto-generated. It is a manually managed config file. The revert commit was erroneous — the dev agent confused it with an auto-generated file.

---

## Acceptance Criteria Verification

| Criteria | Status |
|----------|--------|
| 1. No `check_type: "signup_login_flow"` in smoke_tests | ✅ (after QC fix) |
| 2. Entry `auth-signup-login-flow` has `check_type: "http_200"` | ✅ (after QC fix) |
| 3. JSON is well-formed (`node -e "JSON.parse(...)"`) | ✅ |
| 4. Change committed and pushed | ✅ commit `a283a257` |
| 5. No new "Auth: signup then login failing" tasks | ⏳ Verify on next heartbeat |

---

## QC Checklist

### Security
- [x] No auth code changes — N/A
- [x] Config change only, no secrets or tokens

### Code Quality
- [x] JSON syntax valid
- [x] Only `project.config.json` modified

### Path & Structure
- [x] No .md files at root
- [x] No junk files (coverage/, node_modules/, .next/)

### Commit Hygiene
- [x] QC commit is scoped: `git add project.config.json`
- [x] Descriptive commit message with context

### Semantic Correctness
- [x] `check_type: "http_200"` exists in CHECK_FUNCTIONS (confirmed from genome/health/smoke-tests.js)
- [x] `url` key is read by `buildTests()` (confirmed from PRD analysis)
- [x] `/login` page returns HTTP 200 (already verified by separate `login-page` smoke test)

---

## Root Cause of Dev Failure

Dev agent ran `git add -A` or `git add .` after implementing the fix, inadvertently staged a second copy of `project.config.json` that matched main (possibly from a stash or workspace merge artifact), then committed it as "restore auto-generated files."

**Lesson:** Dev agents must verify the content of `project.config.json` after committing any "restore" or "chore" commits to ensure they haven't undone deliberate changes.

---

## Files Modified by QC

- `project.config.json` — re-applied the `http_200` fix (commit `a283a257`)
- `docs/reports/QC-REVIEW-4cf2e1a8-93ab-4fa6-926d-2a1f9445799c.md` — this report
