# QC Review: Trial Signup Route — UTM Fields Written to Agent Record

**Task ID:** 8d786883-8352-41ad-8062-120628233b14
**Branch Reviewed:** dev/0d815eb7-dev-fix-database-migration-not-run-utm-c
**PR:** #826 (MERGED)
**Date:** 2026-04-05
**Verdict:** PASS

---

## Summary

The original bug was: trial-signup route did not write UTM fields to the `real_estate_agents` DB record. They only appeared in analytics event logs.

The fix was split across two commits:
1. **Route fix (on main prior to branch cut):** `utm_source`, `utm_medium`, `utm_campaign` were added to the `real_estate_agents` INSERT in `app/api/auth/trial-signup/route.ts` (lines 140-142). This was already done before this dev task.
2. **This dev task (PR #826):** Added the DB migration to create the columns (`migrations/003_add_utm_columns_to_agents.sql`, `supabase/migrations/021_add_utm_columns_to_agents.sql`) and a DB-level E2E test.

---

## Verification Steps

### Commits on branch
- `be8a718 feat: Add UTM columns to real_estate_agents table` — the only dev commit
- Files changed: 2 migration SQL files + 1 E2E test (no route file changes needed — route was already fixed)

### Route file state (confirmed on main)
- Line 73: `utm_source, utm_medium, utm_campaign` destructured from `request.json()`
- Lines 140-142: UTM fields included in `real_estate_agents` INSERT with null-safe fallback (`utm_source || null`)
- This is correct and complete.

### DB columns confirmed present
```
utm_source   | text | nullable
utm_medium   | text | nullable
utm_campaign | text | nullable
utm_content  | text | nullable
utm_term     | text | nullable
```
Plus indices: `idx_real_estate_agents_utm_source`, `idx_real_estate_agents_utm_composite`

### Build check
- `npm run build` in `product/lead-response/dashboard/` — PASS (no errors, only a turbopack lockfile warning)

### Test check
- `npm test` (integrations/test-e2e-flow.js) fails on FUB_API_KEY/Twilio — pre-existing, not caused by this change
- Dev's E2E test (`tests/e2e/utm-columns-migration.test.js`): 20/20 PASS
- QC integration test (`tests/integration/trial-signup-utm.test.js`): 15/15 PASS

### QC E2E Test Written
`tests/integration/trial-signup-utm.test.js` — committed to branch. Covers:
- Route file static analysis (destructuring + INSERT block)
- Migration file existence and content
- DB column types and nullability
- Round-trip INSERT/SELECT with UTM values
- NULL behavior when UTM not provided
- Index existence

---

## Issues Found

**None critical.** One observation:

- The dev task name was "fix-database-migration-not-run-utm-columns-missing" — this is actually a sub-task of the original "UTM not written to agent record" bug. The route fix itself was done prior (not tracked as a separate commit in this branch). The migration alone completes the fix since the columns must exist for the INSERT to work.

- `npm test` fails on FUB/Twilio credentials — pre-existing issue, not introduced by this PR.

---

## Verdict: PASS

The fix is real, complete, and correct:
- UTM fields are written to the `real_estate_agents` INSERT in the route
- DB columns exist and accept the data
- Migration SQL files are present in the repo for reproducibility
- All QC E2E tests pass
