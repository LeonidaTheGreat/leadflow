# QC Review: Genome Phase 1A — Observability
**Task ID:** b68d4d53-a15f-4425-95b2-3f0f151285db  
**Use Case:** genome-phase1a-observability  
**Verdict: ⚠️ PARTIAL — REQUEST CHANGES**  
**Date:** 2026-03-25

---

## Automated Gates

### Gate 1: Dashboard Build
```
FAIL (pre-existing, unrelated to this task)
TypeScript errors in:
  - app/api/leads/sample-status/route.ts:64 — .catch() on PostgrestFilterBuilder
  - app/api/setup/complete/route.ts:71 — .catch() on PostgrestFilterBuilder
  - components/onboarding-wizard-overlay.tsx:377 — Type mismatch on SetupSimulatorProps
Note: These errors were present before PR #499 merged. PR #499 only added docs/prd/genome-phase1a-observability.md.
```

### Gate 2: Tests
```
1 test failed — FUB API 403 (pre-existing API credential issue, not a regression)
```

### Gate 3: No junk files
```
PASS: no coverage/, node_modules/, .next/ committed
```

### Gate 4: No root .md files
```
PASS
```

---

## PR Review

**PR #499** (merged): Added `docs/prd/genome-phase1a-observability.md` — 28 additions, 0 deletions.

The actual implementation changes are in `~/.openclaw/genome/core/` (outside the repo). Reviewed those files directly.

---

## Acceptance Criteria Verification

### ✅ 1. heartbeat-wrapper.js — stdio: 'inherit'
- **Status: IMPLEMENTED**
- Line 95: `stdio: 'inherit'` confirmed
- `stdio: 'pipe'` not present
- Executor output is now visible in logs

### ✅ 2. heartbeat-executor.js — Structured JSON logging
- **Status: IMPLEMENTED**
- `_step(id, action, fn)` method wraps every step
- Logs `{step, action, ts}` at START (for visibility if step crashes before completion)
- Logs `{step, action, outcome, duration_ms}` at END
- `_recordStepOutcome()` tracks all step results
- `_reportStepFatalError()` sends Telegram alert on FATAL step errors

### ✅ 3. heartbeat-executor.js — Fatal step alerts to Telegram
- **Status: IMPLEMENTED (for step-level fatals only)**
- `_reportStepFatalError()` calls `reportToTelegram` with: FATAL label, step ID, classification, error message
- Fires immediately when a fatal step error is classified

### ❌ 4. realtime-dispatcher.js — Telegram alerts for: heartbeat failure, agent crash, budget breach, stale tasks >2h
- **Status: NOT IMPLEMENTED**
- **Heartbeat failure (>60 min):** Watchdog at line ~1402 restarts via launchd but does NOT send Telegram alert
- **Agent crash:** No crash-specific Telegram alert in healthCheck
- **Budget breach (<$2):** Budget check only logs WARN, no Telegram alert
- **Stale ready tasks >2h:** `stale` keyword in dispatcher only refers to payload staleness (lines 346, 830), not task queue staleness
- The PRD claims these were added to `realtime-dispatcher.js` healthCheck — they were **not**

### ❌ 5. /genome-health REST endpoint
- **Status: NOT IMPLEMENTED**
- No `app/api/genome-health/route.ts` in dashboard
- No `/genome-health` route in `server.js`
- PRD documents the endpoint as "Added" but it does not exist anywhere in the codebase

---

## E2E Test Results

```
Test 1: heartbeat-wrapper.js stdio: inherit
  ✅ PASS: stdio: 'inherit' found in heartbeat-wrapper.js
  ✅ PASS: stdio: 'pipe' not found

Test 2: heartbeat-executor.js structured JSON step logging
  ✅ PASS: _step() method exists
  ✅ PASS: Structured JSON logging (step, action) present
  ✅ PASS: Timestamp included
  ✅ PASS: Duration tracking present
  ✅ PASS: Outcome field present

Test 3: heartbeat-executor.js _recordStepOutcome
  ✅ PASS: _recordStepOutcome() method exists

Test 4: Fatal error Telegram alert
  ✅ PASS: _reportStepFatalError() exists
  ✅ PASS: Fatal errors report to Telegram

Test 5: realtime-dispatcher.js Phase 1A Telegram alerts
  ❌ FAIL: No Telegram alert for budget breach
  ❌ FAIL: No Telegram alert when heartbeat >60min (launchd restart only)
  ❌ FAIL: No stale ready tasks >2h Telegram alert in dispatcher

Test 6: /genome-health REST endpoint
  ❌ FAIL: /genome-health endpoint NOT found in dashboard

RESULT: 4 failures / 11 tests
```

Test file: `tests/genome-phase1a-observability.test.js`

---

## QC Checklist

### Security
- [x] No tokens/secrets introduced
- [x] No crypto changes
- [x] No auth bypass
- [x] No hardcoded secrets

### Code Quality (genome core files)
- [x] Error handling: `_step()` has try/catch, errors logged with context
- [x] JSON logging is structured and consistent
- [x] No loose equality issues in new code

### Path & Project Structure
- [x] PRD added to `docs/prd/` (correct location)
- [x] No root-level .md files
- [x] No junk files committed

### Commit Hygiene
- [x] PR #499: Only `docs/prd/genome-phase1a-observability.md` committed
- [x] Clear commit message

---

## Required Changes (Blocking)

1. **Add Telegram alerts to `realtime-dispatcher.js` healthCheck:**
   - When heartbeat not run in >60 min: `this.sendTelegram('🚨 Heartbeat stalled >60min')` (currently only does launchd restart)
   - When budget < $2: `this.sendTelegram('💸 Budget breach: $X remaining')`
   - When tasks stuck in ready >2h: query for ready tasks >2h old, alert with count/IDs

2. **Add `/genome-health` REST endpoint to dashboard:**
   - Create `product/lead-response/dashboard/app/api/genome-health/route.ts`
   - Return JSON: `{last_heartbeat, active_agents, budget_remaining, error_count_24h, stale_ready_count}`
   - Protect with API key or internal-only middleware

3. **Fix pre-existing dashboard build failures** (separate task but blocking CI):
   - `app/api/leads/sample-status/route.ts:64` — replace `.catch()` with `void`
   - `app/api/setup/complete/route.ts:71` — replace `.catch()` with `void`
   - `components/onboarding-wizard-overlay.tsx:377` — fix SetupSimulatorProps type mismatch

---

## Summary

| Item | Status |
|------|--------|
| stdio: inherit | ✅ Done |
| Structured JSON logging | ✅ Done |
| Fatal step Telegram alert | ✅ Done |
| Dispatcher Telegram alerts (4 conditions) | ❌ Missing |
| /genome-health endpoint | ❌ Missing |
| Dashboard build | ❌ Pre-existing failures |

**Verdict: REQUEST CHANGES.** Core logging is in place (items 1–3), but 2 of 5 acceptance criteria are unmet. The PRD falsely claims the dispatcher alerts and genome-health endpoint were implemented.
