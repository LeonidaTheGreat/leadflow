# QC Review: fix-smoke-auth-dashboard-not-deployed

**Task ID:** 61518682-824d-464a-bda0-51de43844180
**Dev Task:** 3fb96da0-caad-4998-bc99-63480d5b4ba0
**PR:** #861 (CLOSED — merge conflict)
**Use Case Status:** needs_merge
**Review Date:** 2026-04-05

## Verdict: FAIL

The task's PR was closed due to merge conflicts and never merged. The production dashboard is currently broken (HTTP 500 on all pages). The core config fix is done but the deployment state is regressed.

---

## Automated Gate Results

| Check | Result |
|-------|--------|
| npm run build (local) | PASS |
| npm test | PASS (FUB/Twilio credentials not set — expected in QC env) |
| No junk files in diff | N/A (PR closed, not merged) |

---

## E2E Test Results

Test file: `tests/qc-61518682-smoke-auth-deploy.test.js`

| Test | Result |
|------|--------|
| project.config.json has no signup_login_flow check_type | PASS |
| login-page smoke test: http_200 with url | PASS |
| signup-page smoke test: http_200 with url | PASS |
| deployed /login returns 200 | FAIL (500) |
| deployed /signup returns 200 | FAIL (500) |
| auth-signup-login-flow entry: if present, uses http_200 | PASS |

---

## What the Dev Agent Did

1. PR #861 was created on branch `dev/3fb96da0-dev-fix-smoke-auth-dashboard-not-deploye`
2. PR contained trial nudge banner code — NOT the smoke auth config fix
3. PR was closed due to merge conflicts with main (code_reviews.status = 'closed', rejected_by = 'merge_conflict')
4. The dev agent DID deploy the correct Next.js dashboard at some point — deployment `leadflow-ps74m0dvo` (34s build, ~25 min ago) returns 200 on /login and /signup
5. A subsequent bad deployment `leadflow-k75yicd26` (9s build, ~10 min ago) overwrote production with only `server.js` — causing FUNCTION_INVOCATION_FAILED on all pages

---

## Root Cause Analysis

### What Passed
- `project.config.json` no longer has `signup_login_flow` check_type (AC1-3 from PRD-V6 met)
- `login-page` and `signup-page` smoke tests correctly use `http_200` with `url` field
- Local build succeeds

### What Failed
- Production deployment is broken: `leadflow-ai-five.vercel.app` serves only `server.js` (webhook server), not the Next.js dashboard
- Deployment `dpl_C91oe71wUaBFW8Yv6YgsvgmkCknw` shows `λ server.js (422.97KB)` — wrong artifact
- All pages including /login, /signup, /pricing return HTTP 500
- PR #861 was closed unmerged (merge conflict)

### Deployment Contamination Pattern
- Correct deployment: `leadflow-ps74m0dvo` (34s build) — Next.js, /login and /signup return 200
- Broken deployment: `leadflow-k75yicd26` (9s build) — just `server.js`, all routes return 500
- The 9s fast builds suggest deployment was run from wrong directory (repo root instead of `product/lead-response/dashboard/`)

---

## Required Fix

1. Re-deploy from the correct directory:
   ```bash
   cd /Users/clawdbot/projects/leadflow/product/lead-response/dashboard
   vercel --prod --scope stojans-projects-7db98187
   ```
2. Verify /login and /signup return 200
3. Ensure no further root-level deploys corrupt the production alias

---

## Security

No security issues found in the config changes. The smoke test config patch is benign.
