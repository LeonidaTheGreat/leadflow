# QC Review: Fix SMS-Stats API Agent Scoping and Column Names

**Task ID:** d7a4783e-306c-40db-a68c-95134b56f6d5  
**Task Name:** fix-sms-stats-api-agent-scoping-and-column-names  
**Commit:** f8f3a51  
**Review Date:** 2026-04-04  
**Reviewer:** QC Agent  

---

## Automated Gates ✅

### 1. Build Check
- **Status:** ✅ PASS
- **Command:** `npm run build` (Next.js dashboard)
- **Output:** Build succeeded with 0 errors

### 2. Test Check
- **Status:** ✅ PASS (No Regression)
- **Main branch baseline:** 23 failed, 56 passed
- **This branch:** 23 failed, 56 passed
- **Change:** 0 regressions
- **Full test suite:** `Test Suites: 23 failed, 1 skipped, 56 passed, 79 of 80 total`

### 3. Junk Files Check
- **Status:** ✅ PASS
- **Check:** No coverage/, node_modules/, .next/, or dist/ files committed
- **Result:** Clean commit

### 4. Root-Level Markdown Files Check
- **Status:** ✅ PASS
- **Check:** No root .md files added (except allowed docs)
- **Result:** All files in correct directories

---

## Manual Code Review ✅

### Change 1: Outbound Message Agent Scoping (Correct ✅)
**File:** `product/lead-response/dashboard/app/api/analytics/sms-stats/route.ts` (lines 70-74)

```typescript
let outboundQuery = supabaseAdmin
  .from('sms_messages')
  .select('id, status, lead_id, leads!inner(agent_id)')
  .in('direction', ['outbound-api', 'outbound-reply'])
  .eq('leads.agent_id', agentId)
```

**Verification:**
- ✅ Queries `sms_messages` table (not `messages`)
- ✅ Uses Supabase `leads!inner(agent_id)` join syntax
- ✅ Filters via `.eq('leads.agent_id', agentId)` instead of directly on sms_messages
- ✅ Uses correct direction values ('outbound-api', 'outbound-reply') per Twilio API

### Change 2: Inbound Message Agent Scoping (Correct ✅)
**File:** `product/lead-response/dashboard/app/api/analytics/sms-stats/route.ts` (lines 118-122)

```typescript
let inboundQuery = supabaseAdmin
  .from('sms_messages')
  .select('lead_id, body, leads!inner(agent_id)')
  .eq('direction', 'inbound')
  .eq('leads.agent_id', agentId)
```

**Verification:**
- ✅ Queries `sms_messages` table
- ✅ Selects `body` column (not `message_body`)
- ✅ Uses Supabase `leads!inner(agent_id)` join syntax
- ✅ Filters agent via `.eq('leads.agent_id', agentId)`
- ✅ Column name matches actual schema: sms_messages.body

### Change 3: Opt-Out Detection Fix (Correct ✅)
**File:** `product/lead-response/dashboard/app/api/analytics/sms-stats/route.ts` (line 139)

```typescript
const repliedLeadIds = new Set(
  (inboundMessages || [])
    .filter((m: any) => !isOptOut(m.body))  // ← uses m.body, not m.message_body
    .map((m: any) => m.lead_id)
    .filter(Boolean)
)
```

**Verification:**
- ✅ References `m.body` (matches the column selected in line 120)
- ✅ Correctly filters out opt-out keywords: 'stop', 'unsubscribe', 'cancel', 'quit', 'end', 'optout', 'opt-out'
- ✅ isOptOut() properly lowercases and trims before checking: `(body || '').toLowerCase().trim()`
- ✅ Comment updated to reflect correct column name

---

## Security Checklist ✅

- [x] **Tokens/Secrets:** No plaintext tokens or secrets added
- [x] **Crypto Randomness:** Not applicable (no randomness used)
- [x] **Auth Bypass:** Auth check at line 42 `const agentId = await getAuthUserId(request)` prevents unauthorized access
  - Returns 401 if no valid session
  - agent_id obtained from authenticated session, NOT from query params
- [x] **Middleware Enforcement:** Route uses proper auth middleware via `getAuthUserId()`
- [x] **Dead Code:** No debug endpoints or unused code left behind
- [x] **Input Validation:** Window parameter validated (lines 52-56): only '7d', '30d', 'all' accepted
- [x] **Rate Limiting:** Endpoint is behind auth; Vercel managed rate limits apply
- [x] **No eval/innerHTML/SQL Injection:** No dangerous patterns found

---

## Code Quality Checklist ✅

- [x] **Loose Equality:** No `==` or `!=` operators used (all strict `===`/`!==`)
- [x] **Null Checks:** Proper null/undefined guards throughout
- [x] **Error Handling:** Try/catch at handler level, proper error logging for each query
- [x] **No Hardcoded Values:** Window defaults to 30d (configurable via param), no hardcoded URLs
- [x] **Import Paths:** All imports resolve correctly to @/lib/* aliases
- [x] **Response Contract:** API returns consistent shape with fields documented in comments

---

## Semantic Correctness ✅

- [x] **Table References:** sms_messages has correct columns per actual schema
  - id ✅
  - lead_id ✅
  - direction ✅
  - body ✅ (NOT message_body)
  - status ✅
  - created_at ✅
- [x] **Join Syntax:** Supabase `leads!inner(agent_id)` syntax correct for foreign key filtering
- [x] **Column Names:** `body` matches actual schema (not `message_body`)
- [x] **Agent Scoping:** All three queries (outbound, inbound, bookings) properly filter via leads.agent_id join

---

## Test Coverage ✅

**Existing Tests:** E2E test file updated in same commit  
**Test Status:** 23 failed tests (pre-existing), 56 passed (no regression)  
**Coverage:**
- ✅ Table selection verified
- ✅ Column references verified
- ✅ Agent scoping verified
- ✅ Opt-out filtering verified
- ✅ Auth checks verified

---

## Deployment Verification ✅

- **Build Status:** ✅ Next.js build successful
- **No Breaking Changes:** Compatible with existing database schema
- **Backward Compatibility:** Fixes query accuracy without changing API response format
- **Vercel Deployment:** Code ready for production (Next.js Route Handler in `/app/api/`)

---

## Summary

### ✅ APPROVED - All Criteria Met

**What was fixed:**
1. **Outbound message agent scoping:** Now uses `leads!inner(agent_id)` join instead of nonexistent `agent_id` column on sms_messages
2. **Inbound message agent scoping:** Now uses `leads!inner(agent_id)` join and correct `body` column
3. **Opt-out detection:** Now references `m.body` instead of nonexistent `m.message_body` column

**Quality Assessment:**
- ✅ All automated gates pass (build, tests, no regression)
- ✅ Security controls properly implemented
- ✅ Code quality high (no loose equality, proper error handling, input validation)
- ✅ Semantic correctness verified against actual database schema
- ✅ No deployment blockers

**Approval Decision:** **APPROVED FOR MERGE**

The implementation correctly addresses all three requirements specified in the PRD. The API now properly scopes analytics data to authenticated agents and uses correct column names from the actual sms_messages schema.

---

**QC Approval:** ✅ Approved  
**Recommended Action:** Merge to main and deploy to production

