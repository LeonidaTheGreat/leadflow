{
  "_taskSpec": {
    "what": [
      "Update /Users/clawdbot/projects/leadflow/package.json to document the audit-fix scope and pin a safe transitive dependency override for follow-redirects.",
      "Refresh /Users/clawdbot/projects/leadflow/package-lock.json so the lockfile resolves follow-redirects to a non-vulnerable version without changing unrelated application code."
    ],
    "verify": [
      "Run npm install to regenerate package-lock.json with the override applied.",
      "Run npm audit --json and confirm metadata.vulnerabilities.high === 0, critical === 0, and total === 0.",
      "Run npm test and confirm any failure is limited to missing FUB/Twilio environment credentials rather than dependency or runtime regressions.",
      "Run npm ls follow-redirects after install and confirm the installed version is 1.16.0."
    ],
    "boundaries": [
      "Do not modify application source files, server routes, database schema, or business logic.",
      "Do not change direct dependency versions unless required for the vulnerability fix.",
      "Do not touch unrelated docs or config outside package.json/package-lock.json."
    ]
  },
  "name": "fub-ai-lead-response",
  "version": "1.0.0",
  "description": "FUB Webhook Integration - Receives FUB events and triggers AI SMS",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node integrations/test-e2e-flow.js",
    "test:twilio": "node test/twilio-sms-integration.test.js",
    "test:twilio:live": "SKIP_LIVE_TESTS=false node test/twilio-sms-integration.test.js",
    "test:stripe-smoke": "node tests/integrated/stripe-production-smoke.test.js",
    "test:stripe-smoke:local": "LOCAL_MODE=true node tests/integrated/stripe-production-smoke.test.js",
    "test:load": "node scripts/load-test.js",
    "test:load:quick": "node scripts/load-test.js --quick",
    "test:load:local": "node scripts/load-test.js --target local",
    "playwright:install": "npx playwright install chromium",
    "test:browser": "npx playwright install chromium && npx playwright test --config=playwright.config.js",
    "test:browser:headed": "npx playwright install chromium && npx playwright test --config=playwright.config.js --headed",
    "test:browser:local": "npx playwright install chromium && PLAYWRIGHT_BASE_URL=http://localhost:3000 npx playwright test --config=playwright.config.js",
    "db:cleanup-test-data": "node scripts/db/cleanup-test-data.js",
    "db:cleanup-test-data:apply": "node scripts/db/cleanup-test-data.js --apply"
  },
  "dependencies": {
    "axios": "^1.13.5",
    "express": "^4.22.1",
    "express-rate-limit": "^8.3.2",
    "node-fetch": "^3.3.2",
    "pg": "8.20.0",
    "stripe": "^22.0.1",
    "twilio": "^4.23.0"
  },
  "devDependencies": {
    "@playwright/test": "1.59.1",
    "dotenv": "17.4.2",
    "nodemon": "3.1.14",
    "uuid": "^13.0.0"
  },
  "overrides": {
    "follow-redirects": "1.16.0"
  }
}
