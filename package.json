{
  "_taskSpec": {
    "what": [
      "Update /Users/clawdbot/projects/leadflow/package.json to keep the documented npm audit fix scope current and raise the direct dev dependency uuid from ^13.0.0 to ^14.0.0.",
      "Refresh /Users/clawdbot/projects/leadflow/package-lock.json so the resolved dependency graph keeps follow-redirects at 1.16.0 and installs uuid 14.0.0 without touching application logic."
    ],
    "verify": [
      "Run npm install to regenerate package-lock.json with the updated uuid version and existing follow-redirects override applied.",
      "Run npm audit --json and confirm metadata.vulnerabilities.high === 0, critical === 0, and total === 0.",
      "Run npm test and confirm the test suite completes without dependency-resolution or runtime regressions.",
      "Run npm ls follow-redirects uuid after install and confirm follow-redirects is 1.16.0 and uuid is 14.0.0."
    ],
    "boundaries": [
      "Do not modify application source files, server routes, database schema, or business logic.",
      "Do not change any dependencies other than the documented audit-fix packages required to clear npm audit findings.",
      "Do not touch unrelated docs or config outside package.json/package-lock.json."
    ]
  },
  "name": "fub-ai-lead-response",
  "version": "1.0.0",
  "description": "FUB Webhook Integration - Receives FUB events and triggers AI SMS",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm --prefix product/lead-response/dashboard run build",
    "lint": "eslint .",
    "test": "node scripts/test-suite-gate.js",
    "test:twilio": "node test/twilio-sms-integration.test.js",
    "test:twilio:live": "SKIP_LIVE_TESTS=false node test/twilio-sms-integration.test.js",
    "test:stripe-smoke": "node tests/integrated/stripe-production-smoke.test.js",
    "test:stripe-smoke:local": "LOCAL_MODE=true node tests/integrated/stripe-production-smoke.test.js",
    "test:load": "node scripts/load-test.js",
    "test:load:quick": "node scripts/load-test.js --quick",
    "test:load:local": "node scripts/load-test.js --target local",
    "playwright:install": "npx playwright install chromium",
    "test:browser": "npx playwright install chromium && npx playwright test --config=playwright.config.js",
    "test:browser:headed": "npx playwright install chromium && npx playwright test --config=playwright.config.js --headed",
    "test:browser:local": "npx playwright install chromium && PLAYWRIGHT_BASE_URL=http://localhost:3000 npx playwright test --config=playwright.config.js",
    "clean_worktree": "node scripts/clean-worktree.js",
    "db:cleanup-test-data": "node scripts/db/cleanup-test-data.js",
    "db:cleanup-test-data:apply": "node scripts/db/cleanup-test-data.js --apply",
    "no_direct_db": "node scripts/no-direct-db.js",
    "completion_reports": "node scripts/tasks/completion-reports-retention.js"
  },
  "dependencies": {
    "axios": "^1.13.5",
    "express": "^4.22.1",
    "express-rate-limit": "^8.3.2",
    "node-fetch": "^3.3.2",
    "pg": "8.20.0",
    "stripe": "^22.0.1",
    "twilio": "^4.23.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.37.0",
    "@playwright/test": "1.59.1",
    "dotenv": "17.4.2",
    "eslint": "^9.37.0",
    "globals": "^16.4.0",
    "nodemon": "3.1.14",
    "uuid": "^14.0.0"
  },
  "overrides": {
    "follow-redirects": "1.16.0"
  }
}
